from setuptools import setup, find_packages

setup(
    name="genpc-plus",
    version="0.2.0",
    description="Compact zero-shot Pixal--MoGe point-cloud completion pipeline",
    packages=find_packages(),
    python_requires=">=3.8",
)
